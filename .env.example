# Placeholder guidance only. Supply sensitive values from your environment or secret manager.
# UAGENT_SEED - set outside the repository for hosted mode.
# ASI_API_KEY - optional; set outside the repository if needed.
